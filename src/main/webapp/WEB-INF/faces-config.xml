<?xml version="1.0" encoding="UTF-8"?>
<faces-config xmlns="https://jakarta.ee/xml/ns/jakartaee"
              xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                                  https://jakarta.ee/xml/ns/jakartaee/jakartaee_web_faces_3_0.xsd"
              version="4.0">

    <!-- Configuration globale de l'application JSF -->
    <application>
        <message-bundle>com.example.messages</message-bundle>
        <locale-config>
            <default-locale>fr</default-locale>
            <supported-locale>en</supported-locale>
        </locale-config>
    </application>

    <!-- Définition d'un Bean managé -->
    <managed-bean>
        <managed-bean-name>utilisateurBean</managed-bean-name>
        <managed-bean-class>com.example.beans.UtilisateurBean</managed-bean-class>
        <managed-bean-scope>session</managed-bean-scope>
    </managed-bean>

    <!-- Configuration de la navigation -->
    <navigation-rule>
        <from-view-id>/login.xhtml</from-view-id>
        <navigation-case>
            <from-outcome>success</from-outcome>
            <to-view-id>/home.xhtml</to-view-id>
            <redirect/>
        </navigation-case>
    </navigation-rule>

    <!-- Définition d'un convertisseur personnalisé -->
    <converter>
        <converter-id>dateConverter</converter-id>
        <converter-class>com.example.converters.DateConverter</converter-class>
    </converter>

</faces-config>
